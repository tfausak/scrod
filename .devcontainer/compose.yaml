services:
  devcontainer:
    command: nix-shell --command 'echo ready && sleep infinity' --pure
    image: nixos/nix:2.33.1
    init: true
    volumes:
      - ..:/workspace
    working_dir: /workspace
